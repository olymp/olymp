
  /*setTimeout(() => {
    const db = server.adapter.client;
    db.collection('termin').createIndex({ id: 1 }, { unique: true });
    db.collection('termin').createIndex({ slug: 1 }, { unique: true });
    db.collection('einrichtung').createIndex({ id: 1 }, { unique: true });
    db.collection('einrichtung').createIndex({ slug: 1 }, { unique: true });
    db.collection('artikel').createIndex({ id: 1 }, { unique: true });
    db.collection('artikel').createIndex({ slug: 1 }, { unique: true });
  }, 1000);*/
